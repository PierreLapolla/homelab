{
  "extends": ["config:base"],
  "regexManagers": [
    {
      "fileMatch": ["^clusters/my-cluster/helmreleases/.*\\.yaml$"],
      "matchStrings": ["version: \"(?<currentValue>.*?)\""],
      "depNameTemplate": "{{ packageFilePath }}",
      "datasourceTemplate": "helm",
      "versioningTemplate": "semver"
    }
  ]
}
